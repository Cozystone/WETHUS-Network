# 2026-08-14

- Work continues only in `C:\0.ASKIM ALL-VIN\0.6.WETHUS-Network`; the production-connected WETHUS repository remains untouched.
- Removed the three Network home AI suggestion boxes and refined the compact AI composer typography.
- Connected the local backend to the PC's Ollama `dolphin3:latest` model.
- Added an original actor-scoped temporal knowledge graph memory with provenance, persistence, recall, export, and deletion.
- Memory now covers WETHUS profiles, projects, tasks, activities, team messages, progress snapshots, resources, integrations, connections, applications, bookmarks, and AI conversation episodes.
- Verified in the in-app browser that a follow-up question recalls the prior answer and that grounding is generated only from stored WETHUS facts rather than model-written citations.
- Upgraded the mentor from lexical recall plus one response into a contextual reasoning pipeline: broad project candidates, an intent/situation/causal model, conditional named-reference resolution, focused causal checks, and a separate natural response stage.
- Old inline `[Activity]`/`[Task]` provenance strings are migrated to a compact context label; detailed provenance remains in API metadata instead of being pasted into the visible answer.
- Actual `dolphin3:latest` QA connected a named follow-up to the exact team message and correctly deferred an unrelated visual proposal because it did not produce the missing evidence or change the failed workflow step.
- Created and pushed backup branch `backup/pre-home-service-integration-20260814` before the service-wide integration work.
- Turned the signed-in Network home into a working service hub: shared project tasks, shared schedules, public people discovery, connection requests, public profiles, ASK/OFFER, semantic activity, project switching, and project-hub deep links.
- Added `network.html` as the detailed people/connections/requests/schedule/activity surface while preserving the existing WETHUS header and visual system.
- Added a project-hub schedule card so home, Network schedule, and project hub all render the same project schedule state.
- Added frontend and backend attachment ingestion for WETHUS AI. A focused document reasoning stage now distinguishes stated intent from observed behavior and measured results before combining the document with the full project context.
- Deduplicated repeated AI attachments in both project materials and the temporal knowledge graph.
- Browser-verified desktop and 390px mobile flows, task state round trips, schedule round trips, public profile navigation, connection request/cancel, and an Ollama attachment answer grounded in the actual interview document.
- Added `scripts/smoke-network-integration.js`; both network integration and agent-memory smoke tests pass.
- `validate-static` CRLF parsing was fixed. Its only remaining failure is the existing expired opportunity feed, which needs a fresh verified opportunity rather than fabricated data.
