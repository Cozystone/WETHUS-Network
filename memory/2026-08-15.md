# 2026-08-15

- Preserved `C:\0.ASKIM ALL-VIN\0.5.WETHUS` unchanged and continued work only in `C:\0.ASKIM ALL-VIN\0.6.WETHUS-Network`.
- Created rollback branch `backup/pre-natural-home-flow-20260815` before editing the Network home flow.
- Integrated schedule, activity, and ASK/OFFER into the signed-in home's scrollable center column; routine schedule clicks no longer navigate to `network.html`.
- Removed local regex/canned mentor replies. Every chat turn now goes through the configured AI model.
- Added model-native context planning: one model pass decides context scope, selectors choose relevant conversation/project records, and the final model writes a natural answer from a plain-language factual memo.
- Raw knowledge-graph JSON is never sent after the user question; the current message is last and authoritative, preventing data-structure dumps.
- Project action metadata is extracted after the answer without rewriting it, and only project-contextual turns update hub/activity state.
- Kept temporal knowledge-graph memory as the source pool for substantive project questions while casual conversation uses no project evidence.
- Verified browser interactions and all relevant smoke suites. Static validation still reports only the known stale opportunity feed.
